# Activity-protocol host + its core dependency, pinned to the published beta
# releases on PyPI. Bump these when a newer beta is needed.
azure-ai-agentserver-activity==1.0.0b1

# Microsoft 365 Agents SDK
microsoft-agents-hosting-core
microsoft-agents-authentication-msal
microsoft-agents-activity

# The M365 SDK connector imports aiohttp but does not declare it; pin explicitly.
aiohttp>=3.9.0

# Azure SDKs
azure-identity>=1.17.0

# Bridge to the existing agent's Responses endpoint + OBO (x-ms-user-token)
azure-ai-projects>=1.0.0
openai>=1.40.0
PyJWT>=2.8.0
httpx>=0.27.0
azure-storage-blob>=12.15.0

# Utilities
python-dotenv>=1.0.0

# debugpy enables local debugging of this agent with the Foundry Toolkit VS Code
# extension.
debugpy

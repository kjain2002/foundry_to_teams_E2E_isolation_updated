# azd — per-environment secrets (never commit user state)
.azure/*/
.azure/*.env
# Local .env overrides
.env
# Built Teams package (regenerated by build_package.py with your BOT_ID/APP_ID)
agent-activity-teams.zip
teams-app-package/manifest.json
teams-app-package/*.png
# Python
__pycache__/
*.pyc
.venv/
# Response scratch
_publish_resp.json
